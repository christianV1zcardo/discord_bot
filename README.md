# Discord Meme Bot# Discord Meme Bot



Este proyecto es un bot de Discord que responde con memes aleatorios usando la [Meme API](https://meme-api.com/gimme).Este proyecto es un bot de Discord que responde con memes aleatorios usando la API https://meme-api.com/gimme.



## Uso## Uso



Instala las dependencias:1. Instala las dependencias:

   ```bash

```bash   pip install discord requests

pip install discord.py requests   ```

```2. Coloca tu token de bot en el archivo `bot.py`.

3. Ejecuta el bot:

Coloca tu token de bot en una variable de entorno llamada `DISCORD_TOKEN`.   ```bash

   python bot.py

Ejecuta el bot:   ```

4. En tu servidor de Discord, escribe `$meme` para recibir un meme.

```bash

python bot.py## Archivos principales

```- `bot.py`: Código fuente del bot.

- `.gitignore`: Archivos y carpetas ignorados por git.

En tu servidor de Discord, escribe `$meme` para recibir un meme.

## Notas

## Archivos principales- Si la API de memes está caída, el bot no podrá enviar imágenes.

- No compartas tu token de bot públicamente.

- `bot.py`: Código fuente del bot.
- `.gitignore`: Archivos y carpetas ignorados por git.

## Notas

- Si la API de memes está caída, el bot no podrá enviar imágenes.
- No compartas tu token de bot públicamente.
